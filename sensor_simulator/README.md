# Sensor Simulator

## How to compile it

```bash
mkdir build
cd build
cmake ..
cmake build .
```

## Visual Studio Code (VSC) recommended extentions
- C/C++ Extension Pack
- CMake
- CMake Tools
- clangd

###
CMake: Delete Cache and Reconfigure within VSC helps if header files are not recognized
